# Required
# Raw token only — do NOT include "Bot " prefix (the MCP server adds it automatically)
CLAUDEBOT_DISCORD_TOKEN=your-bot-token-here
CLAUDEBOT_DISCORD_GUILD_ID=123456789012345678

# Optional - Poll timeout in seconds (default: 30)
CLAUDEBOT_POLL_TIMEOUT=30

# Optional - Max consecutive poll failures before exit (default: 5)
CLAUDEBOT_MAX_FAILURES=5
