# Required
# Raw token only — do NOT include "Bot " prefix (the MCP server adds it automatically)
CLAUDEBOT_DISCORD_TOKEN=your-bot-token-here
CLAUDEBOT_DISCORD_GUILD_ID=123456789012345678

# Optional - MCP daemon HTTP port (default: 8080, must be unique per instance)
CLAUDEBOT_MCP_PORT=8080

# Optional - Poll timeout in seconds (default: 30)
CLAUDEBOT_POLL_TIMEOUT=30

# Optional - Max consecutive poll failures before exit (default: 5)
CLAUDEBOT_MAX_FAILURES=5

# Optional - Log level (DEBUG, INFO, WARN, ERROR; default: INFO)
CLAUDEBOT_LOG_LEVEL=INFO

# Optional - Docker platform override (e.g. linux/amd64, linux/arm64)
# Omit or leave empty for auto-detect (uses host architecture)
CLAUDEBOT_DOCKER_PLATFORM=

# Optional - Working directory for claude CLI (where .claude/memory/ lives)
# Default: the plugin directory (where claudebot-ctl lives)
# CLAUDEBOT_PROJECT_DIR=/path/to/project
