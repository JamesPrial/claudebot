{
  "mcpServers": {
    "discord": {
      "type": "stdio",
      "command": "docker",
      "args": [
        "run", "--rm", "-i",
        "--pull", "always",
        "-e", "CLAUDEBOT_DISCORD_TOKEN",
        "-e", "CLAUDEBOT_DISCORD_GUILD_ID",
        "ghcr.io/jamesprial/claudebot-mcp:latest",
        "--stdio"
      ]
    }
  }
}
